[build]
builder = "railpack"
buildCommand = "npm ci && npm run build"

[deploy]
startCommand = "sh -c 'until npx prisma migrate deploy; do sleep 2; done && npm run start'"
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 10