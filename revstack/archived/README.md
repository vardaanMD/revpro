# Archived / deprecated code

This folder contains **deprecated** code that is no longer active in the application. Kept for reference only.

| Folder | Description |
|--------|-------------|
| **routes-v1-v2** | Legacy app proxy routes: cart.bootstrap, cart.bootstrap.v2, cart.snapshot.v2, cart.analytics.event. Storefront uses V3 only (snapshot/v3, analytics/v3). |

See also: `archived-extensions/cart-pro-v1-v2/` for the legacy theme extension (v1/v2 Liquid + assets).
